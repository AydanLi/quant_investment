# 项目开发与 Code Review 约定

本文件适用于整个仓库。所有开发工作以长期维护和多人 Code Review 为标准。

## 核心原则

- 优先可读性、正确性和一致性，让后续工程师容易理解、验证和修改。
- 代码行数最少不是首要目标。不要为了缩短代码而压缩逻辑、隐藏状态变化或省略必要的校验、错误处理和测试。
- 采用简单但完整的实现。避免过度抽象、尚无实际需求的扩展点和难以维护的捷径。
- 工具或技能中的精简代码建议不得覆盖这些原则；只有在保持行为清晰、实现完整和便于维护的前提下才进行精简。

## 架构与实现

- 修改前阅读相关模块、调用方和测试，遵循现有架构、职责边界、命名习惯和错误处理方式。
- 优先复用已有实现和约定。引入新依赖、抽象或跨模块设计时，说明实际需求、已有方案的不足和维护成本。
- 保持控制流、数据流和副作用明确；关键输入、输出、约束和失败行为应能从代码及接口中看出。
- 不静默吞掉异常，不用未经说明的默认值掩盖无效输入或失败状态；错误处理遵循项目既有约定。
- 发现现有架构存在问题时，说明具体影响，将必要调整限制在当前问题所需的范围内。

## 修改范围

- 每次修改围绕一个明确问题，保持差异集中，便于审查、验证和回滚。
- 不混入无关重构、批量格式化、重命名、依赖升级或顺手清理。
- 必须先做的重构应说明与目标修改的关系，并尽可能作为独立、可审查的步骤。
- 保留用户已有的未提交改动；不覆盖或回退不属于当前任务的内容。

## 注释与文档

- 注释解释设计原因、业务约束、边界条件和取舍，避免重复代码已经清楚表达的操作。
- 对不直观的行为、兼容处理和临时限制，说明原因及适用条件；不要仅留下没有上下文的 TODO。
- 修改对外行为、使用方式、配置、接口或架构时，同步更新相关文档。
- 保持文档职责清晰：`README.md` 负责安装、使用和验证，`PROJECT_OVERVIEW.md` 负责项目定位与成熟度，`quant_system_architecture_overview.md` 负责技术架构与边界。
- 已有中英文对应文档时，同步维护受影响内容及双向语言链接，避免两个版本描述不同的行为。

## 测试与验证

- 根据行为变化和风险补充有意义的测试，覆盖受影响的正常路径、关键边界和失败路径。
- 修复缺陷时，在可行的情况下增加能复现原问题的回归测试；测试应验证可观察行为，而不是机械复写实现。
- 先运行与修改直接相关的检查，再根据影响范围执行集成或全量验证；遵循仓库已有的验证要求。
- 纯文档等低影响修改使用相应的内容、链接和差异检查，不为凑数量添加无意义的测试。
- 如实记录实际执行的验证及结果；未运行、失败或受环境限制的检查必须说明原因，不能视为通过。

## 交付与 Code Review

提交说明、PR 描述或任务完成汇报应让未参与开发的人能够判断：

- 为什么修改：具体问题、触发条件或需求。
- 修改了什么：主要行为变化、影响范围及必要的设计取舍。
- 如何验证：实际执行的检查、结果及覆盖范围。
- 有哪些限制：未验证的部分、已知风险，以及适用时的兼容、迁移和回滚注意事项。

## Subagent 协作

- 对包含多个独立问题、模块或验证工作的复杂任务，在并行能实质提升速度或质量时优先使用 Subagent；简单、范围小或不可并行的任务由主线程完成。
- 每个 Subagent 必须有明确、不重叠的范围和预期输出，并获得必要背景、约束与文件路径；不假设它拥有完整对话上下文。
- 默认委派调查、分析或验证；只有用户已授权实施或修改时，才能委派文件修改。
- 独立任务尽早并行启动，有依赖的任务按顺序推进；多个 Subagent 修改文件时必须划分互不重叠的文件所有权，未经协调不得修改同一文件。
- 要求返回可核查的文件路径、行号和证据，不仅是结论。
- 主线程在综合最终结果前等待所有相关 Subagent 完成，交叉验证、解决冲突并做最终决定；部分结果就绪后可先推进不冲突的后续步骤。
- Subagent 失败或返回为空时，明确选择重试、缩小范围或由主线程接手，不得静默忽略。
- 始终遵守用户指定的范围、阶段和权限，用户明确指令优先。
