# Quant System v3 项目简介

语言：中文 | [English](PROJECT_OVERVIEW_EN.md)

更新日期：2026-08-26

## 项目定位

Quant System v3 是一个面向单人使用的 ETF 资产轮动研究平台。它把数据质量、
回测、参数准入、风险控制和模拟执行放在同一条可审计链路中，目标是回答：

1. 这个策略结论使用了哪一版代码、数据和 ETF 宇宙？
2. 参数是在什么训练/验证边界内选择的，是否看过未来数据？
3. 收益是否包含现实的成本、现金、结算和风险约束？
4. 当数据、账户或执行证据不完整时，系统是否会拒绝继续？

项目当前是个人研究工具，不是自动交易机器人，也没有获得实盘准入。

## 策略轮廓

- 研究对象：24 只风险 ETF，加现金 ETF `BIL`；内部现金记为 `CASH_USD`。
- 资产范围：美国大小盘、海外股票、国债/信用债、黄金/商品/REIT 和行业 ETF。
- 核心信号：20/60/120 日动量与低波动排名，原始加权动量必须大于 0。
- 调仓：正式候选固定为月频，T 日收盘后计算，T+1 原始开盘执行。
- 组合：选择前 3/4/5 只，目标波动率 8%/10%/12%，单只风险资产目标权重
  10%-35%；不能分配的资金进入 `BIL` 或 `CASH_USD`。
- 区制：VIX 与长期趋势决定防守、基准或慢速风险配置。
- 基准风险模型：sample covariance；dynamic-factor 必须在核心策略冻结后独立准入。
- 研究成本：2/7/20 bp 情景，0.1% ADV 后计入平方根冲击，超过 1% ADV 阻断。

这些参数组成固定的 135 个核心候选，不允许在看完结果后无限搜索“最优参数”。

## 平台解决的问题

### 1. 数据可信

系统保存供应商原始 OHLCV、分红、拆股、证券元数据和修订记录，并在本地按固定
算法重建总回报价格。Tiingo/Yahoo 的 ETF 差异和 CBOE/FRED 的 VIX 差异通过
统一质量门检查。

数据裁决只能针对一个具体问题指纹，并绑定原始快照和哈希。旧快照不修改；裁决后
创建新的派生快照。任何未解决阻断问题都会继续阻止准入和订单。

### 2. 回测可信

回测使用持股数量和现金账本，不假设每日免费再平衡。信号与成交至少相隔一个交易
时段，缺少 T+1 Open、活跃持仓价格或 BIL 基准时失败关闭。交易成本、滑点、冲击、
现金缓冲和 T+1 结算均进入账本。

### 3. 研究治理

研究协议锁定代码提交、数据快照、宇宙版本、候选参数、成本情景、折叠日期、基准
和选择规则。准入使用至少五年训练历史和 12 个月外层测试窗，全部 135 个候选结果
必须留痕。只有通过全部门槛的 `AdmissionRun` 才能冻结 `StrategyVersion`。

### 4. 模拟执行与风控

本地 `REPLAY_OPEN` 将不可变信号、人工批准、订单、成交、账户、结算、对账和事故
状态保存在 SQLite 中。稳定的 cycle/order/execution ID 防止重启后重复成交。

15% 高水位回撤会生成下一交易日清仓草案；恢复至少等到下一月度调仓、完成对账并
获得人工授权。5% 单日损失只停机、不自动清仓，最早下一交易日人工恢复。

## 当前成熟度

| 阶段 | 目标 | 当前状态 |
|---|---|---|
| 数据可信 | 可复现的可执行快照 | **阻断**：5 个快照全部 `BLOCKED` |
| 回测可信 | T+1、现金、成本和前视测试通过 | 工程实现和测试通过 |
| 历史准入 | 完整运行固定 135 候选 | 尚未开始 |
| 本地未来模拟 | 冻结策略后的未来留出记录 | 尚未启动时钟 |
| Broker paper | 真实报价、订单和成交偏差 | 尚未接入 |
| 小额实盘 | 1 万美元现金账户、逐单人工批准 | 未准入，日期无法确定 |

截至 2026-08-26 的数据库证据：

- 数据快照 5 个，全部 `BLOCKED`；
- 2 条 XLF 2016 事件窄范围裁决，覆盖 2,640 个衍生阻断问题；
- 最新快照仍有 706 个未解决问题：收盘差异 588、公司行动金额差异 85、
  公司行动缺失 9、极端收益 24；
- `UV-001` 为 `draft`，且 `historical_universe_integrity=false`；
- 策略版本、准入运行、参数试验、信号决策、模拟账户、订单、成交、对账和事故均为 0；
- 11 个旧实验标记为 `invalid_data_v1`，不能进入比较榜单或准入。

因此，当前最重要的结论不是“策略收益如何”，而是“尚无符合系统自身标准的可信
历史结论”。这不是异常绕过理由，而是质量门正在正常工作。

## 适用范围

适合：

- 研究 ETF 轮动策略的金融合理性和稳健性；
- 验证数据、回测、准入和风控工程；
- 在策略冻结后积累单人本地模拟记录；
- 为未来 broker paper/人工实盘保留可审计接口。

不适合或尚未实现：

- 自动连接券商并下真实订单；
- 盘中实时行情或新闻事件交易；
- 证明真实限价成交质量或 7 bp 真实成本；
- 税后收益、wash sale 和产品税务特征的完整模拟；
- 声称 2006 年以来 ETF 宇宙完全无幸存者偏差。

## 准入路线

```text
解决数据阻断
  -> 产生 actionable DatasetSnapshot
  -> 人工批准 UniverseVersion
  -> 冻结研究协议并运行 135 候选
  -> AdmissionRun: ADMITTED 或 REJECTED
  -> 仅 ADMITTED 才能冻结 StrategyVersion
  -> 启动未来本地模拟时钟
  -> 至少 12 个月 / 12 次调仓 / 30 笔 broker-paper 成交
  -> 再评估 1 万美元人工批准实盘
```

本地 raw-open replay 不计入 broker-paper 成交门槛。真实资金日期必须由未来证据决定，
不能从工程完成日倒推。

## 项目原则

- 证据优先于净值曲线。
- 数据和治理默认失败关闭。
- 历史已看过的区间不伪装成未见样本。
- 所有参数试验，包括失败结果，都必须留痕。
- 模拟、broker paper 和实盘是三个不同证据等级。
- 人工批准是运行约束，不以自动化便利为由绕过。
- 当前历史结果统一为税前 `CURRENT_UNIVERSE_BACKCAST`。

## 进一步阅读

- 使用与验证：[README](README.md)
- 技术设计：[架构说明](quant_system_architecture_overview.md)
- 运行流程：[升级与运行手册](docs/upgrade_v3_runbook.md)
- 已确认约束：[个人研究运行档案](docs/personal_research_operating_profile.md)

本项目不构成投资、税务或法律建议。
